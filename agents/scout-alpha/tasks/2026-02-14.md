# Scout Alpha — Daily Task List
## Date: 2026-02-14 (Saturday)

### 🎯 Today's Mission
Find 10 SME prospects with high call handling pain points.

### Sources to Scrape
Priority order:
1. **Reddit r/smallbusiness** — "missed calls", "receptionist", "phone anxiety"
2. **Reddit r/entrepreneur** — "customer service", "hiring help"
3. **Twitter** — "hiring receptionist", "missed calls SME"
4. **Hacker News** — "show HN" call handling tools

### Usage (NO BROWSER - simple HTTP)
```bash
# Reddit API is JSON, no auth needed for public
curl -s "https://www.reddit.com/r/smallbusiness/search.json?q=missed+calls&limit=25"

# Parse with node
node -e '
const https = require("https");
https.get("https://www.reddit.com/r/smallbusiness.json", (res) => {
  let data = "";
  res.on("data", chunk => data += chunk);
  res.on("end", () => {
    const posts = JSON.parse(data).data.children;
    const leads = posts.filter(p => 
      /missed calls|receptionist|phone/i.test(p.data.selftext)
    );
    console.log(JSON.stringify(leads, null, 2));
  });
}).on("error", e => console.error(e.message));
'
```

### Scoring Criteria
| Signal | Criteria | Count |
|--------|----------|-------|
| **Hot** | Mentions budget >$300/mo OR "losing sales" OR explicit pain | Target: 2 |
| **Warm** | Researching solutions, asking questions | Target: 5 |
| **Cold** | Competitor mention, vague interest | Target: 3 |

### Output
Save to: `../memory/2026-02-14/prospects.json`

### Status
- [ ] 08:00: Run 1 — 10 prospects found ✅
- [ ] 12:00: Run 2 — FAILED (path bug) ❌
- [ ] 16:00: Run 3 — FAILED (truncated payload) ❌
- [ ] 20:00: Run 4 — PENDING (fix command)

### Notes
- 08:23 run successful — found pest control ($750/mo), nail salon, small business
- Need to simplify scraping command — avoid complex pipes in cron payload
